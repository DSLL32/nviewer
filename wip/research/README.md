# Research notes, copied at wind-down (2026-09-11)

Verbatim copies of each interrupted research lead's `WIP.md`, taken from
`~/.ai-tmp/r49/<game>/WIP.md`. They are kept as written, so:

- **Paths point into the scratch directories** (draft specs, extracted files,
  tools, emulator captures). AGENTS.md "Scratch directories" maps game to
  directory. On their own these copies can't restore the research; they record
  where each game stood.
- **The offline renderer they refer to** as `~/.ai-tmp/r49/bm/tools/`
  (`raster.ts`, `compare.ts`, `png.ts`, `sheet.ts`) now also lives in the repo at
  `tools/render/`, which is the copy to use.
- **The scratch `WIP.md` wins** if a resumed lead updates it.

| File | Game | Scratch directory |
|---|---|---|
| `worldisnotenough.md` | The World Is Not Enough | `~/.ai-tmp/r49/twine/` |
| `glover.md` | Glover | `glover/` |
| `marioparty.md` | Mario Party (J) | `mparty/` |
